export function MyTeamSchematicPlayer({
                                          isSelected,
                                          isActive,
                                          position
                                      }: { isSelected: boolean, isActive: boolean, position: number }) {

    return (
        <div>
        </div>
    )
}