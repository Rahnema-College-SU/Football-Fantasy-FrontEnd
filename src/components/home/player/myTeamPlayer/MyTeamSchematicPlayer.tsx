export function MyTeamSchematicPlayer({
                                          isSelected,
                                          isActive
                                      }: { isSelected: boolean, isActive: boolean }) {

    return (
        <div>
        </div>
    )
}