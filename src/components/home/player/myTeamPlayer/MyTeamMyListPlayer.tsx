export function MyTeamMyListPlayer({
                                       isSelected,
                                       isActive,
                                       position
                                   }: { isSelected: boolean, isActive: boolean, position: number }) {
    return (
        <div>

        </div>
    )
}