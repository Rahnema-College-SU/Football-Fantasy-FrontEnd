export function MyTeamListPlayer({
                                     isSelected,
                                     isActive
                                 }: { isSelected: boolean, isActive: boolean }) {
    return (
        <div>

        </div>
    )
}