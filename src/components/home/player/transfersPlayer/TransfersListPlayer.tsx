export function TransfersListPlayer({
                                        isSelected,
                                        isActive
                                    }: { isSelected: boolean, isActive: boolean }) {

    return (
        <div>
        </div>
    )
}