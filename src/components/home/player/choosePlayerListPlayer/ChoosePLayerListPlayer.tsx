export function ChoosePLayerListPlayer({
                                           isSelected,
                                           isActive
                                       }: { isSelected: boolean, isActive: boolean }) {

    return (
        <div>
        </div>
    )
}